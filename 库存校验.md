# createCartWorkflow 与 completeCartWorkflow 库存校验分工差异

## 概述

在 Medusa 电商系统中，购物车的创建和完成流程采用了**两步式库存校验策略**，分别对应不同的业务场景和风险控制需求：

1. **创建购物车时**：使用 `confirmVariantInventoryWorkflow` 进行**可售校验**
2. **完成购物车时**：使用 `reserveInventoryStep` 进行**资源占用**

这两个步骤在购物车生命周期中扮演着不同的角色，共同确保库存的一致性和系统的可靠性。

---

## 1. 创建购物车：confirmVariantInventoryWorkflow（可售校验）

### 1.1 调用位置

在 `createCartWorkflow` 中，当获取商品变体信息后，立即调用 `confirmVariantInventoryWorkflow`：

```typescript
// create-carts.ts:171-178
confirmVariantInventoryWorkflow.runAsStep({
  input: {
    sales_channel_id: salesChannel.id,
    variants: variants as unknown as ConfirmVariantInventoryWorkflowInputDTO["variants"],
    items: input.items!,
  },
})
```

### 1.2 工作流结构

`confirmVariantInventoryWorkflow` 是一个相对简单的工作流，主要包含两个步骤：

```typescript
// confirm-variant-inventory.ts:147-161
export const confirmVariantInventoryWorkflow = createWorkflow(
  confirmVariantInventoryWorkflowId,
  (
    input: WorkflowData<ConfirmVariantInventoryWorkflowInputDTO>
  ): WorkflowResponse<ConfirmVariantInventoryWorkflowOutput> => {
    const confirmInventoryInput = transform(
      { input },
      prepareConfirmInventoryInput
    )

    confirmInventoryStep(confirmInventoryInput)

    return new WorkflowResponse(confirmInventoryInput)
  }
)
```

### 1.3 核心逻辑：prepareConfirmInventoryInput

这个函数是库存校验的关键，它负责：

1. **解析销售渠道与库存位置的关系**：
   - 遍历商品变体的库存配置
   - 找到与当前销售渠道关联的库存位置
   - 确保商品在当前销售渠道下有可售的库存位置

2. **计算可用库存**：
   ```typescript
   // prepare-confirm-inventory-input.ts:131-138
   const availability = MathBN.sub(
     location_levels.raw_stocked_quantity ??
       location_levels.stocked_quantity ??
       0,
     location_levels.raw_reserved_quantity ??
       location_levels.reserved_quantity ??
       0
   )
   ```
   - 可用库存 = 总库存 - 已预留库存

3. **验证库存充足性**：
   - 对于每个商品，检查是否有足够的可用库存
   - 如果 `allow_backorder` 为 true，则跳过库存检查

### 1.4 核心逻辑：confirmInventoryStep

这是实际执行库存确认的步骤：

```typescript
// confirm-inventory.ts:63-101
export const confirmInventoryStep = createStep(
  confirmInventoryStepId,
  async (data: ConfirmVariantInventoryStepInput, { container }) => {
    if (!data.items?.length) {
      return new StepResponse([], [])
    }

    const inventoryService = container.resolve<IInventoryService>(
      Modules.INVENTORY
    )

    // TODO: Should be bulk
    const promises = data.items.map(async (item) => {
      if (item.allow_backorder) {
        return true
      }

      const itemQuantity = MathBN.mult(item.quantity, item.required_quantity)

      return await inventoryService.confirmInventory(
        item.inventory_item_id,
        item.location_ids,
        itemQuantity
      )
    })

    const inventoryCoverage = await promiseAll(promises)

    if (inventoryCoverage.some((hasCoverage) => !hasCoverage)) {
      throw new MedusaError(
        MedusaError.Types.NOT_ALLOWED,
        `Some variant does not have the required inventory`,
        MedusaError.Codes.INSUFFICIENT_INVENTORY
      )
    }

    return new StepResponse(null)
  }
)
```

### 1.5 可售校验的本质

**定义**：可售校验是指系统在用户添加商品到购物车时，检查商品是否有足够的库存可供销售，但**不实际占用库存**。

**核心特点**：
1. **只读操作**：不修改任何库存数据
2. **基于快照**：检查的是当前时间点的库存状态
3. **快速响应**：为用户提供即时的库存可用性反馈
4. **无锁机制**：不使用分布式锁，性能更高

**防止的问题**：
- **无货商品误购**：防止用户将已经缺货的商品添加到购物车
- **销售渠道不匹配**：确保商品在当前销售渠道下有对应的库存位置
- **用户体验优化**：在购物车阶段就告知用户库存不足，避免在结账时才发现

**业务场景**：
- 用户浏览商品，添加到购物车
- 用户修改购物车中商品的数量
- 系统需要快速响应用户的操作

---

## 2. 完成购物车：reserveInventoryStep（资源占用）

### 2.1 调用位置

在 `completeCartWorkflow` 中，当订单创建后，在 `parallelize` 块中调用 `reserveInventoryStep`：

```typescript
// complete-cart.ts:594-606
parallelize(
  createRemoteLinkStep(linksToCreate),
  updateCartsStep([updateCompletedAt]),
  reserveInventoryStep(formatedInventoryItems),  // 库存预留
  registerUsageStep(promotionUsage),
  emitEventStep({
    eventName: OrderWorkflowEvents.PLACED,
    data: { id: createdOrder.id },
    options: {
      priority: EventPriority.CRITICAL,
    },
  })
)
```

### 2.2 核心逻辑：reserveInventoryStep

这是实际执行库存预留的步骤，包含正向操作和补偿操作：

```typescript
// reserve-inventory.ts:61-116
export const reserveInventoryStep = createStep(
  reserveInventoryStepId,
  async (data: ReserveVariantInventoryStepInput, { container }) => {
    if (!data.items.length) {
      return new StepResponse([], {
        reservations: [],
        inventoryItemIds: [],
      })
    }

    const inventoryService = container.resolve(Modules.INVENTORY)
    const locking = container.resolve(Modules.LOCKING)

    const inventoryItemIds: string[] = []

    const items = data.items.map((item) => {
      inventoryItemIds.push(item.inventory_item_id)

      return {
        line_item_id: item.id,
        inventory_item_id: item.inventory_item_id,
        quantity: MathBN.mult(item.required_quantity, item.quantity),
        allow_backorder: item.allow_backorder,
        location_id: item.location_ids[0],
      }
    })

    const lockingKeys = Array.from(new Set(inventoryItemIds))

    // 使用分布式锁确保原子性
    const reservations = await locking.execute(lockingKeys, async () => {
      return await inventoryService.createReservationItems(items)
    })

    return new StepResponse(reservations, {
      reservations: reservations.map((r) => r.id),
      inventoryItemIds,
    })
  },
  // 补偿函数：如果工作流失败，释放预留的库存
  async (data, { container }) => {
    if (!data?.reservations?.length) {
      return
    }

    const inventoryService = container.resolve(Modules.INVENTORY)
    const locking = container.resolve(Modules.LOCKING)

    const inventoryItemIds = data.inventoryItemIds
    const lockingKeys = Array.from(new Set(inventoryItemIds))

    await locking.execute(lockingKeys, async () => {
      await inventoryService.deleteReservationItems(data.reservations)
    })

    return new StepResponse()
  }
)
```

### 2.3 资源占用的本质

**定义**：资源占用是指系统在用户完成购物车（即下单）时，**实际锁定并预留库存**，确保这些库存不会被其他用户购买。

**核心特点**：
1. **写入操作**：创建库存预留记录，修改库存状态
2. **分布式锁**：使用 `locking.execute` 确保操作的原子性
3. **补偿机制**：包含补偿函数，如果工作流失败，自动释放预留的库存
4. **订单关联**：预留记录与订单项关联，便于后续跟踪和管理

**防止的问题**：
- **超售（Overselling）**：这是最核心的问题。当多个用户同时购买同一商品时，如果没有库存预留机制，可能会导致卖出的商品数量超过实际库存。
- **并发冲突**：在高并发场景下，确保库存操作的原子性和一致性
- **订单履约保障**：确保订单创建后，有足够的库存可以发货

**业务场景**：
- 用户点击"提交订单"按钮
- 系统需要确保订单中的商品有足够的库存
- 防止其他用户同时购买同一商品

---

## 3. 为什么需要两步式库存校验？

### 3.1 业务流程分析

理解两步式库存校验的关键在于理解购物车的生命周期：

```
用户浏览商品 → 添加到购物车（可售校验）→ 修改购物车（可售校验）→ 提交订单（资源占用）→ 支付完成 → 发货（扣减库存）
```

在这个流程中，有两个关键的库存决策点：

1. **购物车阶段**：用户可以添加、修改、删除商品，这个阶段可能持续很长时间（几分钟到几天）
2. **订单阶段**：用户确认购买，提交订单，这个阶段需要快速完成

### 3.2 可售校验的必要性

**问题**：为什么不在创建购物车时就预留库存？

**答案**：因为购物车阶段具有以下特点：
- **不确定性**：用户可能会放弃购物车，不完成购买
- **长时间性**：购物车可能存在几分钟、几小时甚至几天
- **频繁修改**：用户可能会多次修改购物车中的商品数量

如果在创建购物车时就预留库存，会导致：
1. **库存被无效锁定**：大量库存被放弃的购物车占用，影响其他用户购买
2. **资源浪费**：系统需要维护大量的库存预留记录
3. **用户体验差**：商品显示"缺货"，但实际上是被其他用户的购物车占用

**解决方案**：
- 购物车阶段只进行**可售校验**，告诉用户当前是否有货
- 不实际预留库存，让库存保持可用状态
- 当用户真正提交订单时，再进行**资源占用**

### 3.3 资源占用的必要性

**问题**：既然创建购物车时已经检查过库存，为什么提交订单时还要预留？

**答案**：因为在购物车创建到订单提交的这段时间内，库存状态可能已经发生变化：

1. **时间差问题**：
   - 用户A在10:00添加商品到购物车，此时库存有10件
   - 用户B在10:05也添加同一商品到购物车，此时库存还有10件
   - 用户A在10:10提交订单，如果没有预留机制，用户B也可以提交订单
   - 结果：卖出了2件，但库存只有10件（这里例子不够明显，应该是库存只有1件的情况）

2. **并发问题**：
   - 假设库存只有1件
   - 用户A和用户B同时提交订单
   - 如果没有原子性的库存预留机制，两个订单都可能成功
   - 结果：超售1件

**解决方案**：
- 提交订单时，使用**分布式锁**确保库存预留的原子性
- 实际创建库存预留记录，增加 `reserved_quantity`
- 如果工作流失败，通过**补偿机制**释放预留的库存

---

## 4. 关键区别对比

### 4.1 核心差异表

| 维度 | confirmVariantInventoryWorkflow（可售校验） | reserveInventoryStep（资源占用） |
|------|--------------------------------------------|---------------------------------|
| **调用时机** | 创建购物车、添加商品、修改数量时 | 提交订单、完成购物车时 |
| **操作类型** | 只读操作 | 写入操作 |
| **是否修改库存** | 否 | 是（创建预留记录） |
| **使用分布式锁** | 否 | 是 |
| **是否有补偿机制** | 否 | 是 |
| **性能开销** | 低 | 较高 |
| **时效性** | 基于当前快照 | 实际锁定 |
| **失败影响** | 用户无法添加商品到购物车 | 订单创建失败 |

### 4.2 数据模型差异

**可售校验关注的数据**：
- `stocked_quantity`：总库存数量
- `reserved_quantity`：已预留数量
- `available_quantity`：可用数量 = 总库存 - 已预留
- `sales_channel`：销售渠道与库存位置的关联

**资源占用操作的数据**：
- 创建 `Reservation` 记录：
  - `inventory_item_id`：库存项目ID
  - `line_item_id`：订单行项目ID
  - `quantity`：预留数量
  - `location_id`：库存位置ID
  - `allow_backorder`：是否允许欠货

### 4.3 错误处理差异

**可售校验的错误处理**：
- 库存不足时，抛出 `MedusaError.Types.NOT_ALLOWED` 错误
- 错误代码：`MedusaError.Codes.INSUFFICIENT_INVENTORY`
- 用户体验：立即看到"库存不足"的提示
- 系统影响：工作流终止，购物车未创建

**资源占用的错误处理**：
- 库存不足时，由 `inventoryService.createReservationItems` 内部处理
- 可能的错误：
  - 库存不足
  - 分布式锁获取失败
  - 数据库操作失败
- 补偿机制：如果后续步骤失败，自动调用补偿函数释放预留
- 用户体验：订单创建失败，需要重新尝试

---

## 5. 实际场景分析

### 5.1 场景一：正常购买流程

**用户行为**：
1. 用户A浏览商品X，库存10件
2. 用户A添加1件商品X到购物车
3. 系统执行 `confirmVariantInventoryWorkflow`：
   - 检查可用库存：10 - 0 = 10 ≥ 1
   - 校验通过，购物车创建成功
4. 用户A继续浏览，10分钟后提交订单
5. 系统执行 `reserveInventoryStep`：
   - 获取分布式锁
   - 创建库存预留记录
   - 释放分布式锁
6. 订单创建成功，库存预留完成

**系统状态**：
- `stocked_quantity`：10（不变）
- `reserved_quantity`：1（增加）
- `available_quantity`：9（减少）

### 5.2 场景二：并发购买（库存紧张）

**用户行为**：
1. 商品Y库存只有1件
2. 用户A和用户B同时在购物车中添加了商品Y
3. 两个用户的 `confirmVariantInventoryWorkflow` 都通过了（因为此时还没有预留）
4. 用户A先点击"提交订单"：
   - 系统执行 `reserveInventoryStep`
   - 获取分布式锁
   - 检查库存：1 - 0 = 1 ≥ 1
   - 创建预留记录，`reserved_quantity` 变为1
   - 释放分布式锁
   - 订单A创建成功
5. 用户B随后点击"提交订单"：
   - 系统执行 `reserveInventoryStep`
   - 获取分布式锁
   - 检查库存：1 - 1 = 0 < 1
   - 库存不足，操作失败
   - 订单B创建失败

**结果**：
- 只有用户A成功购买，避免了超售
- 用户B收到"库存不足"的提示

### 5.3 场景三：购物车放弃

**用户行为**：
1. 用户C添加商品Z到购物车
2. `confirmVariantInventoryWorkflow` 通过
3. 用户C关闭浏览器，放弃购物车
4. 3天后，购物车自动过期（或用户手动删除）

**系统状态**：
- 没有创建任何库存预留记录
- 商品Z的库存状态完全不受影响
- 其他用户可以正常购买商品Z

**对比**：
- 如果创建购物车时就预留库存，那么这3天内商品Z的库存会被无效锁定
- 通过两步式校验，避免了这种资源浪费

### 5.4 场景四：订单取消（补偿机制）

**用户行为**：
1. 用户D提交订单，购买商品W
2. `reserveInventoryStep` 执行成功，创建了预留记录
3. 后续步骤（如支付授权）失败
4. 工作流触发补偿机制
5. `reserveInventoryStep` 的补偿函数被调用：
   - 获取分布式锁
   - 删除之前创建的预留记录
   - 释放分布式锁
6. 库存被释放，商品W恢复可售状态

**系统状态**：
- `reserved_quantity` 从1恢复为0
- `available_quantity` 从9恢复为10
- 没有超售，也没有库存被无效锁定

---

## 6. 技术实现细节

### 6.1 库存计算逻辑

**可用库存计算**：
```typescript
// prepare-confirm-inventory-input.ts:131-138
const availability = MathBN.sub(
  location_levels.raw_stocked_quantity ??
    location_levels.stocked_quantity ??
    0,
  location_levels.raw_reserved_quantity ??
    location_levels.reserved_quantity ??
    0
)
```

- 使用 `raw_` 前缀的字段优先，这是原始的 BigNumber 值
- 如果没有原始值，则使用普通的数值字段
- 可用库存 = 总库存 - 已预留库存

**预留数量计算**：
```typescript
// reserve-inventory.ts:79-86
const items = data.items.map((item) => {
  inventoryItemIds.push(item.inventory_item_id)

  return {
    line_item_id: item.id,
    inventory_item_id: item.inventory_item_id,
    quantity: MathBN.mult(item.required_quantity, item.quantity),
    allow_backorder: item.allow_backorder,
    location_id: item.location_ids[0],
  }
})
```

- 实际预留数量 = 购买数量 × 每个数量所需的库存单位
- 例如：如果 `required_quantity` 是2（表示每个购买单位需要2个库存单位），购买数量是3，那么实际预留6个库存单位

### 6.2 分布式锁机制

**锁的使用**：
```typescript
// reserve-inventory.ts:88-92
const lockingKeys = Array.from(new Set(inventoryItemIds))

const reservations = await locking.execute(lockingKeys, async () => {
  return await inventoryService.createReservationItems(items)
})
```

- 锁定的键是 `inventory_item_id` 的集合
- 使用 `locking.execute` 确保在锁的保护下执行库存预留操作
- 防止并发情况下的库存超售

**锁的作用**：
1. **原子性**：确保"检查库存-创建预留"是一个原子操作
2. **隔离性**：不同的库存操作之间互不干扰
3. **一致性**：保证库存数据的一致性

### 6.3 补偿机制

**补偿函数的设计**：
```typescript
// reserve-inventory.ts:99-115
async (data, { container }) => {
  if (!data?.reservations?.length) {
    return
  }

  const inventoryService = container.resolve(Modules.INVENTORY)
  const locking = container.resolve(Modules.LOCKING)

  const inventoryItemIds = data.inventoryItemIds
  const lockingKeys = Array.from(new Set(inventoryItemIds))

  await locking.execute(lockingKeys, async () => {
    await inventoryService.deleteReservationItems(data.reservations)
  })

  return new StepResponse()
}
```

- 补偿函数接收正向操作返回的 `StepResponse` 中的补偿数据
- 补偿数据包含：
  - `reservations`：创建的预留记录ID列表
  - `inventoryItemIds`：涉及的库存项目ID列表
- 补偿操作：
  - 同样使用分布式锁确保原子性
  - 删除之前创建的预留记录
  - 释放库存

**补偿触发条件**：
- 工作流中任何后续步骤失败
- 工作流被手动取消
- 系统异常导致工作流中断

---

## 7. 设计思想总结

### 7.1 分层架构

Medusa 的库存管理采用了清晰的分层架构：

1. **业务层（Workflows）**：
   - `createCartWorkflow`：购物车创建流程
   - `completeCartWorkflow`：购物车完成流程
   - 负责业务流程编排和错误处理

2. **服务层（Services）**：
   - `InventoryModuleService`：库存核心服务
   - 提供 `confirmInventory`、`createReservationItems` 等方法
   - 封装库存业务逻辑

3. **数据层（Data Access）**：
   - 库存项目、库存位置、预留记录等实体
   - 负责数据持久化和查询

### 7.2 最终一致性

Medusa 使用工作流引擎来确保最终一致性：

1. **步骤化执行**：将复杂的业务流程拆分为多个步骤
2. **补偿机制**：每个步骤都可以有对应的补偿操作
3. **重试机制**：工作流引擎支持自动重试失败的步骤
4. **幂等性设计**：关键操作（如订单创建）设计为幂等，避免重复执行

### 7.3 性能与一致性的平衡

两步式库存校验是性能与一致性之间的精妙平衡：

| 设计决策 | 性能优势 | 一致性保障 |
|---------|---------|-----------|
| 购物车阶段只读 | 快速响应，无锁开销 | 基于快照的一致性 |
| 订单阶段加锁写入 | 只在关键路径加锁 | 强一致性，防止超售 |
| 补偿机制 | 失败时自动回滚 | 确保最终一致性 |

### 7.4 用户体验考虑

从用户体验角度看，两步式校验也有明显优势：

1. **快速反馈**：
   - 添加商品到购物车时立即知道是否有货
   - 不需要等待复杂的库存预留操作

2. **清晰的错误信息**：
   - 购物车阶段："商品库存不足"
   - 订单阶段："由于库存变化，订单创建失败，请重试"

3. **降低挫败感**：
   - 不在购物车阶段锁定库存，让更多用户有机会购买
   - 只在真正提交订单时才锁定，提高库存利用率

---

## 8. 代码引用

以下是本文提到的关键代码文件和位置：

### 8.1 工作流定义

- **createCartWorkflow**：`/packages/core/core-flows/src/cart/workflows/create-carts.ts:113-278`
- **completeCartWorkflow**：`/packages/core/core-flows/src/cart/workflows/complete-cart.ts:302-669`
- **confirmVariantInventoryWorkflow**：`/packages/core/core-flows/src/cart/workflows/confirm-variant-inventory.ts:147-161`

### 8.2 步骤实现

- **confirmInventoryStep**：`/packages/core/core-flows/src/cart/steps/confirm-inventory.ts:63-101`
- **reserveInventoryStep**：`/packages/core/core-flows/src/cart/steps/reserve-inventory.ts:61-116`

### 8.3 工具函数

- **prepareConfirmInventoryInput**：`/packages/core/core-flows/src/cart/utils/prepare-confirm-inventory-input.ts:89-214`

---

## 9. 总结

Medusa 的两步式库存校验策略是一个经过精心设计的解决方案，它：

### 核心原则

1. **可售校验（购物车阶段）**：
   - **目的**：快速响应用户，提供库存可用性反馈
   - **动作**：只读检查，计算 `可用库存 = 总库存 - 已预留`
   - **防止**：无货商品误购，销售渠道不匹配
   - **本质**：基于快照的一致性检查

2. **资源占用（订单阶段）**：
   - **目的**：实际锁定库存，防止超售
   - **动作**：创建预留记录，增加 `reserved_quantity`
   - **防止**：并发超售，库存不一致
   - **本质**：基于锁的强一致性操作

### 设计智慧

这种设计体现了以下软件工程智慧：

1. **关注点分离**：将"检查"和"占用"两个关注点分离，各自优化
2. **性能优化**：在非关键路径（购物车）使用轻量级操作，在关键路径（订单）使用重量级操作
3. **用户体验**：快速反馈 + 清晰的错误信息
4. **系统可靠性**：补偿机制 + 分布式锁 + 幂等设计

### 实际价值

对于电商系统来说，库存管理的核心挑战是：
- **高并发**：多个用户同时购买同一商品
- **一致性**：不能超售，也不能库存显示错误
- **用户体验**：快速响应，清晰的反馈

Medusa 的两步式库存校验策略完美地平衡了这些需求，是一个值得学习和借鉴的设计范例。